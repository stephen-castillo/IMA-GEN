import React from 'react'

const FormField = ({
  name,
  labelName,
  isSurpriseMe,
  handleSurpriseMe,
  type,
  placeholder,
  value,
  handleChange,
}) => (
  <div>
  FormField
  </div>
);

export default FormField
